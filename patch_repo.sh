sed -i 's/class AttendanceRepositoryImpl(/import android.content.Context\nimport androidx.work.OneTimeWorkRequestBuilder\nimport androidx.work.WorkManager\nimport androidx.work.Constraints\nimport androidx.work.NetworkType\nimport com.example.data.sync.AttendanceSyncWorker\n\nclass AttendanceRepositoryImpl(/' app/src/main/java/com/example/data/repository/AttendanceRepositoryImpl.kt
sed -i 's/private val attendanceDao: AttendanceDao/private val attendanceDao: AttendanceDao,\n    private val context: Context/' app/src/main/java/com/example/data/repository/AttendanceRepositoryImpl.kt
sed -i '/attendanceDao.insertRecords(records.map { it.toEntity() })/a\
\        val constraints = Constraints.Builder().setRequiredNetworkType(NetworkType.CONNECTED).build()\n        val syncRequest = OneTimeWorkRequestBuilder<AttendanceSyncWorker>().setConstraints(constraints).build()\n        WorkManager.getInstance(context).enqueue(syncRequest)' app/src/main/java/com/example/data/repository/AttendanceRepositoryImpl.kt
sed -i '/attendanceDao.insertPermission(permission.toEntity())/a\
\        val constraints = Constraints.Builder().setRequiredNetworkType(NetworkType.CONNECTED).build()\n        val syncRequest = OneTimeWorkRequestBuilder<AttendanceSyncWorker>().setConstraints(constraints).build()\n        WorkManager.getInstance(context).enqueue(syncRequest)' app/src/main/java/com/example/data/repository/AttendanceRepositoryImpl.kt
