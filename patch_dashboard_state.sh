sed -i '/val isCR = uiState.user?.role?.name == "CR"/d' app/src/main/java/com/example/ui/dashboard/StudentDashboard.kt
sed -i '/val items = if (isCR) listOf/d' app/src/main/java/com/example/ui/dashboard/StudentDashboard.kt
sed -i '/val icons = if (isCR) listOf/d' app/src/main/java/com/example/ui/dashboard/StudentDashboard.kt
sed -i '/val uiState by viewModel.uiState.collectAsState()/a\    val isCR = uiState.user?.role == com.example.domain.model.Role.CR\n    val items = if (isCR) listOf("Home", "Attendance", "Academics", "Messages", "CR Duty", "Profile") else listOf("Home", "Attendance", "Academics", "Messages", "Profile")\n    val icons = if (isCR) listOf(Icons.Filled.Home, Icons.Filled.DateRange, Icons.Filled.MenuBook, Icons.Filled.Message, Icons.Filled.Security, Icons.Filled.Person) else listOf(Icons.Filled.Home, Icons.Filled.DateRange, Icons.Filled.MenuBook, Icons.Filled.Message, Icons.Filled.Person)' app/src/main/java/com/example/ui/dashboard/StudentDashboard.kt
